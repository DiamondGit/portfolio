export const dynamic = "force-dynamic";

export default function SSR() {
    return (
        <div>
            <h1>Server-Side Rendering (SSR)</h1>
            <p>This page is dynamically rendered on the server.</p>
        </div>
    );
}
